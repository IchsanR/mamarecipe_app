import 'package:flutter/material.dart';

void main() {
  runApp(const MamarecipeApp());
}

class MamarecipeApp extends StatelessWidget {
  const MamarecipeApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      title: "Mamarecipe",
    );
  }
}
